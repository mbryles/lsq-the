spring:
    servlet:
        multipart:
            enabled: true
            max-file-size: 2MB
            max-request-size: 2MB
    jpa:
        hibernate:
            dialect: org.hibernate.dialect.MySQL5InnoDBDialect
            ddl-auto: create
    datasource:
        url: jdbc:mysql://localhost:3306/lsq?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=America/New_York
        username: the
        password: secretsecret
        driver-class-name: com.mysql.jdbc.Driver
logging:
    level:
        root: info
        org.springframework: info
        com.blastingconcept.lsq.the: info